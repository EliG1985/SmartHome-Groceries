interface StorePanelProps {
  // No props for now
}

export function StorePanel({}: StorePanelProps) {
  // ...existing code...
  // Minimal placeholder UI
  return (
    <div className="store-panel p-4">
      <h2 className="text-xl font-bold mb-4">Store</h2>
      <p>Store features are currently disabled.</p>
    </div>
  );
}
